name: cached_network_image_platform_interface
description: Platform interface for CachedNetworkImage
version: 4.1.1
homepage: https://github.com/Baseflow/flutter_cached_network_image
publish_to: 'none'

environment:
  sdk: ^3.0.0
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager:
    git:
      url: https://github.com/omegaui/flutter_cache_manager.git
      ref: develop
      path: flutter_cache_manager

dev_dependencies:
  file: '>=7.0.0 <8.0.0'
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
